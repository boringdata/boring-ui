# Sprite runtime environment (example)
#
# Copy to runtime.env (optional) to override injected env vars.
#
# Note: Do NOT put secrets in this file in production images. Prefer platform
# secret stores / injected env.

# HTTP port for the co-hosted runtime backend
PORT=8000

# Control plane base URL (used by the runtime for callbacks, links, etc.)
# CONTROL_PLANE_URL=https://app.example.com

# Optional run-mode toggles (future)
# ENVIRONMENT=staging
# LOG_LEVEL=info

